#include <iostream>
using namespace std;
int fact(int n) // creating function fact
{
	if(n==0)
	return 1;
	else 
	return(n*fact(n-1)); // formula for factorial
}
int main()
{
	int n;
	cout<<"enter a numeber\n";
	cin>>n;
	cout<<"result:\n"<<fact(n);
}
